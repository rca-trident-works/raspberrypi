# coding: utf-8

# 引数として受け取った10はカッコ内の変数aに代入される
# 引数として受け取った40はカッコ内の変数bに代入される
def func(a, b):
    print("引数である変数aの値は" + str(a))
    print("引数である変数bの値は" + str(b))


# func関数の引数に「10」と「40」の値を送る
func(10, 40)

